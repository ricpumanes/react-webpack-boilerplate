import React from "react";
import PropTypes from "prop-types";

const Home = (props) => {
  return (
    <div>
      <div className="h-screen bg-gray-500">home</div>
    </div>
  );
};

Home.propTypes = {};

export default Home;
